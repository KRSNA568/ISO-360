name = "iso-audit360"
compatibility_date = "2026-03-04"

# Cloudflare Pages project configuration
# Deploy: wrangler pages deploy dist --project-name=iso-audit360

[env.production]
  vars = { NODE_ENV = "production" }

[env.staging]
  vars = { NODE_ENV = "staging" }
